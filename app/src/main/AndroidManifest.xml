<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusOne">

        <!-- Entry Point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Authentication -->
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".RegisterActivity"
            android:windowSoftInputMode="adjustResize" />

        <!-- Student Sections -->
        <activity android:name=".ResourcesActivity" />
        <activity android:name=".LostFoundActivity" />
        <activity
            android:name=".ReportLostFoundActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".ClubDetailActivity" />

        <!-- Role Dashboards -->
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".ClubMemberDashboardActivity" />

        <!-- Admin Management Subsections -->
        <activity android:name=".AdminAnnouncementsActivity" />
        <activity android:name=".AdminUsersActivity" />

        <!-- Creation & Editing Forms -->
        <activity
            android:name=".AddEditAnnouncementActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".AddEditEventActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".AddEditResourceActivity"
            android:windowSoftInputMode="adjustResize" />

    </application>

</manifest>